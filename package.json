{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "vite build --",
        "postbuild": "node -e \"const fs=require('fs'); const path=require('path'); const manifest=path.join('public','build','manifest.json'); if(!fs.existsSync(manifest)) { console.error('Vite manifest not found: '+manifest); console.error('Run \\\"npm run build\\\" first to generate it.'); process.exit(1);}\"",
        "dev": "vite"
    },
    "devDependencies": {
        "@alpinejs/focus": "^3.14.0",
        "@tailwindcss/forms": "^0.5.2",
        "@tailwindcss/postcss": "^4.0.0",
        "@tailwindcss/vite": "^4.0.0",
        "alpinejs": "^3.4.2",
        "autoprefixer": "^10.4.2",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "laravel-vite-plugin": "^2.0.0",
        "postcss": "^8.4.31",
        "tailwindcss": "^4.0.0",
        "vite": "^7.0.7"
    }
}
